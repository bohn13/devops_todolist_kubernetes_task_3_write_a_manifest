apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: todoapp
  labels:
    app: busybox-curl
spec:
  containers:
    - name: busybox
      image: ikulyk404/busyboxplus:curl
      ports:
        - containerPort: 8080
      command: ['sh', '-c', "sleep 3600"]